#include "game/Helper.h"

/*
std::string TUI::inventoryTUI() {
    std::string temp = "Map: \n"
    + getMap + "\n\n"
    + "X: " + std::to_string(x) + " | Y: " + std::to_string(y)
    + "\n --- [ Special Message ] ---\n" + message + "\n --- [ Special Message ] ---"
    + "\nHealth: " + std::to_string(playerHealth)
    + "\nEnter command: ";
    return temp;
}

std::string TUI::battleTUI() {
    std::string temp = "Map: \n"
    + getMap + "\n\n"
    + "X: " + std::to_string(x) + " | Y: " + std::to_string(y)
    + "\n --- [ Special Message ] ---\n" + message + "\n --- [ Special Message ] ---"
    + "\nHealth: " + std::to_string(playerHealth)
    + "\nEnter command: ";
    return temp;
}

std::string TUI::itemsTUI() {
    std::string temp = "Map: \n"
    + getMap + "\n\n"
    + "X: " + std::to_string(x) + " | Y: " + std::to_string(y)
    + "\n --- [ Special Message ] ---\n" + message + "\n --- [ Special Message ] ---"
    + "\nHealth: " + std::to_string(playerHealth)
    + "\nEnter command: ";
    return temp;
}
*/